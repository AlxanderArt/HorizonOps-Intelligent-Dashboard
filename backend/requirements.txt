# Core - minimal for Render free tier
fastapi==0.109.0
uvicorn==0.27.0
pydantic==2.5.3

# Authentication (pure Python, no cryptography compilation)
PyJWT==2.8.0

# Form handling
python-multipart==0.0.6
